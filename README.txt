There are to ways to run the server, you can run it locally or through a local host.

To run it locally, simply just open the index.html page in the html folder.

In order to run the server on localhost:3000
enter:
npm i
node runServer.js or npm start
into terminal 
